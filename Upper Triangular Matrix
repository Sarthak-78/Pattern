#include <iostream>
int main(){
    int n;
    std::cin >> n;
    for(int i=n; i>0; i--){
        for(int k=1; k<= (n-i); k++){
            std::cout << " ";
        }
        for(int j=1; j<=i; j++){
            std::cout << '*';
        }
        std::cout << std::endl;
    }
}
